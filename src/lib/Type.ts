
export enum Type {
  FILE = 'File',
  DIRECTORY = 'Directory',
  STRING = 'string',
  BOOLEAN = 'boolean',
  INT = 'int',
  DOUBLE = 'double',
  STRING_ARRAY = 'string[]',
  FLOAT = 'float',
  STDERR = 'stderr',
  STDOUT = 'stdout'
}
