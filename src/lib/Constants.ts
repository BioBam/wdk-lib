import { CWLVersion } from 'cwl-ts-auto';

/**
 * Constants used in the library.
 */
export class Constants {

  /**Version of the CWL specification that this library uses for workflows. */
  // static cwlVersion: string = 'v1.2';
  static cwlVersion: CWLVersion = CWLVersion.V1_2;


}