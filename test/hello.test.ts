// import { Hello } from '../src';

test('hello', () => {
//   expect(new Hello().sayHello()).toBe('hello, world!');
});

test('createWorkflow', () => {
//   expect(new Hello().createToolJson()).toContain('echo');
});